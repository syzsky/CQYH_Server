using System;

namespace 游戏服务器.数据类
{
	[Flags]
	public enum 行会权限
	{
		仓库一取 = 1,
		仓库二取 = 2,
		仓库三取 = 4,
		仓库四取 = 8,
		仓库五取 = 0x10,
		仓库六取 = 0x20,
		仓库一存 = 0x40,
		仓库二存 = 0x80,
		仓库三存 = 0x100,
		仓库四存 = 0x200,
		仓库五存 = 0x400,
		仓库六存 = 0x800
	}
}
